# النسخ الاحتياطي والاستعادة

البيانات في PostgreSQL، وليست في مجلد Next أو localStorage. استخدم حساب backup منفصلًا بصلاحيات قراءة كافية، وPGSERVICE/PGPASSFILE أو secret manager. حساب التطبيق لا يملك تعديل/حذف سجل الأحداث أو حركات المخزون، وحساب المهاجرات منفصل.

## التشغيل المستهدف

- نسخة يومية مشفرة خارج قرص التطبيق، retention مقترح 30 يومًا حسب السعة، مع مراقبة فشل النسخ وإرسال تنبيه تشغيلي وفق أدوات الاستضافة المعتمدة.
- `backend/ops/backup.sh` ينفذ pg_dump custom ثم GPG encryption ويوقف التنفيذ عند فشل أي مرحلة. يتطلب تثبيت PostgreSQL client وGPG وتحديد مفتاح المستلم ومجلد خاص. رفع النسخة خارج الجهاز والـretention يضبطان لدى المزود؛ لم يُفعّلا محليًا كخدمة مجدولة.
- RPO المقترح 24 ساعة للنسخ اليومي؛ PITR لدى المزود إذا كان المطلوب أقل. RTO أربع ساعات هدف يحتاج إثباتًا على حجم العميل وليس وعدًا.
- لا تضع dump أو أسرارًا في public أو Git. الملفات المحلية للاختبار في `.runtime/backups` مستبعدة من Git ولا تحتوي بيانات عميل فعلية.

## اختبار الاستعادة

`node backend/scripts/backup-restore.mjs` يأخذ نسخة من قاعدة اسمها ينتهي `_test` ويستعيدها إلى قاعدة فارغة اسمها ينتهي `_restore`. يرفض الكتابة فوق قاعدة موجودة. يقارن counts لعشرة جداول، وعينات من الطلبات والحجوزات والحركات، ويطابق مجموع الحركات والحجوزات النشطة بالأرصدة.

لتجربة أخرى أنشئ قاعدة restore جديدة مستقلة واضبط `RESTORE_DATABASE_URL`؛ لا يمسح السكربت قاعدة سابقة. الاختبار المحلي يستخدم dump غير مشفر داخل المجلد الخاص لبيانات الاختبار فقط؛ تشفير النسخ الخارجية والجدولة ينتظران بيئة النشر ومفتاح التشفير.

## حادث استعادة

1. أوقف الكتابة وحدد آخر نسخة سليمة وسبب العطل.
2. فك التشفير إلى مكان خاص واستعد بمالك DB إلى قاعدة مستقلة، دون `--clean` على القاعدة الأصلية.
3. طبق منح runtime بعد الاستعادة. نفذ `npm run inventory:reconcile` باستخدام عنوان القاعدة المستعادة، وقارن counts وعينات الطلبات والجلسات وفق سياسة الحادث.
4. تحقق من المهاجرات وصحة الخدمة ثم اختبر القراءة والدخول في بيئة معزولة.
5. وجّه الاتصال إلى القاعدة المستعادة بعد اعتماد مسؤول التشغيل، وأعد تشغيل التطبيق وراقب الأخطاء. وثق الفقد الزمني ومدة الاستعادة الفعلية.
