# تقرير المرحلة الثانية — 12 سبتمبر 2026

## النتائج الفعلية

| الفحص | النتيجة |
|---|---|
| baseline ثم regression للـdomain/mock adapter | 28/28 ناجحة |
| TypeScript للواجهة والباك إند | ناجح |
| ESLint | ناجح بلا أخطاء أو تحذيرات |
| بناء Prisma + Nest + Next للإنتاج | ناجح |
| تكامل PostgreSQL حقيقية بهوية runtime محدودة | 16/16 ناجحة |
| Playwright على بناء الإنتاج وChrome وحسابات حقيقية | 4/4 ناجحة |
| مهاجرات dev/test من الصفر ثم migrate deploy ثانية | ناجحة؛ لا مهاجرات متبقية |
| backup/restore إلى قاعدة مستقلة فارغة | ناجح: counts وعينات وreconciliation |
| npm audit بعد معالجة التبعيات | 0 ثغرات معروفة وقت الفحص |

بيئة التحقق النهائية: Windows، Node 24.19.0، PostgreSQL 18.3 في cluster معزول على loopback:55439، Nest 12.0.1، Prisma 7.10.0، Next 16.3.4. النتائج تثبت السلوك المحلي ولا تعني نشرًا أو SLA.

## تغطية التكامل

- B01–B03: دخول صحيح/خاطئ، session opaque وhash محفوظ، إلغاء جلسات بعد password change/reset/logout، منع CSRF وOrigin مخالف.
- B04–B08: خصوصية المسودات والطلبات، counts/search بلا تسرب، ظهور المرسل للإدارات قبل دورها، اعتماد جزئي/كامل/رفض، رفض decisions مكررة/ناقصة، منع المخزن قبل المراجعة.
- B09–B14: طلبان على آخر رصيد؛ واحد ينجح والآخر يفشل. آخر بند ناقص يرجع كل الحجز. نفس key متزامن يعيد نتيجة واحدة؛ body مختلف وversion قديمة يرفضان.
- B15–B20: حجز ثم صرف واحد ثم تسليم دون خصم إضافي، سائق آخر مرفوض، تعذر ثم تسليم مع بقاء التاريخ، إلغاء ينافس الخروج بنتيجة متسقة، وتعطيل السائق المسند إليه يرفض خلال سباق الخروج.
- B21–B25: snapshots محفوظة، آخر Admin يبقى نشطًا تحت تعديلين متنافسين، الجرد دون المحجوز والافتتاح المكرر مرفوضان، الرصيد يطابق الدفتر، وDTO بلا hashes أو sessions.
- B26: فلاتر الفترة وpageSize والتواريخ غير الصالحة؛ يوم تغيير توقيت القاهرة 2026-04-24 يمتد 23 ساعة، وحد اليوم التالي exclusive.
- B27: إيقاف وتشغيل process API مستقل مع نفس DB؛ الطلبات والجلسة تبقى. UTC timestamp ضمن الوقت الفعلي. runtime لا يسمح UPDATE للدفتر أو DELETE للسجل. حذف المسودة يقبل retry لنفس النتيجة.
- B28–B29: رحلة UI فعلية عبر الأدوار والموبايل ثم الحسابات وrefresh. لا localStorage كقاعدة ولا زر demo في الإنتاج.
- B30: pg_dump custom ثم pg_restore إلى sales_restore فارغة دون تغيير المصدر.

## سيناريوهات المتصفح

1. إنشاء مستخدم من الإدارة، كلمة مرور مؤقتة مرة واحدة، نموذج التغيير الإجباري، ثم reset من الإدارة.
2. قطع الاستجابة بعد نجاح POST في DB؛ يظهر الفشل وتبقى المدخلات، وretry يعود لنفس ID بدل مسودة ثانية.
3. إنشاء → مراجعة جزئية → حجز → تعيين → خروج → تسليم موبايل → متابعة الحسابات. لا استثناء JavaScript في الرحلة.
4. صفحات العملاء والمنتجات والمستخدمين والسجل والملف، ثم استلام مخزون وظهوره في دفتر الحركات.

تقرير المتصفح في `playwright-report-api` ولقطات `test-results`، واللقطات العامة المختارة تحت `docs/screenshots/api-*`. قد يحتوي trace على بيانات حسابات الاختبار؛ التقارير والمخرجات المؤقتة مستبعدة من Git.

## الاستعادة المقاسة

النسخة المستعادة: 18 مستخدمًا، عميل واحد، 6 منتجات، 38 طلبًا، 43 بندًا، 6 أرصدة، 12 حجزًا، 26 حركة، 5 محاولات تسليم، و160 حدثًا. counts في 10 جداول وعينات الطلبات والحجوزات والحركات تطابقت، وفروق reconciliation صفر. زمن dump+restore والتحقق المحلي لهذا الحجم الصغير: 838ms؛ ليس تقدير RTO لبيانات العميل.

## الحدود والملاحظات

- عولج اختلاف تحويل timestamptz في driver بضبط اتصال API إلى UTC، وأضيف اختبار يمنع رجوع الانحراف.
- اختبارات أولية اختارت صنف التنافس المستنفد؛ صُححت fixtures إلى SKUs المعروفة. منع الحجز عند النقص كان صحيحًا. تبديل الحساب ينتظر اكتمال logout.
- لا SQLite أو repository mock في اختبارات معاملات الباك إند. اختبارات المرحلة الأولى الـ28 منفصلة عن دليل سلامة DB.
- لم يُختبر Safari/Firefox أو قارئ شاشة أو طابعة فعلية. لم يُجر حمل staging لـ10,000 طلب/20 مستخدمًا. workspace وسجل الحركة قراءات تجميعية تحتاج قياسًا عند التوسع.
- لم تُفعّل جدولة النسخ المشفر خارج الجهاز أو المراقبة لدى مزود. السكربتات والتوثيق جاهزان، والمفتاح والوجهة والـretention يحددان عند النشر.
- لا نشر فعلي ولا بيانات عملاء حقيقية ولا ترحيل تلقائي لـmock. متطلبات بيئة الإنتاج المتبقية في DEPLOYMENT.md.

تقرير الواجهة السابق محفوظ في `docs/FRONTEND_PHASE1_TEST_REPORT.md`.
